<p id="notice"><%= notice %></p>

<h1>Tipocontratos</h1>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipocontratos.each do |tipocontrato| %>
      <tr>
        <td><%= tipocontrato.tipo %></td>
        <td><%= link_to 'Show', tipocontrato %></td>
        <td><%= link_to 'Edit', edit_tipocontrato_path(tipocontrato) %></td>
        <td><%= link_to 'Destroy', tipocontrato, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipocontrato', new_tipocontrato_path %>
