<%= form_with(model: tipocontrato, local: true) do |form| %>
  <% if tipocontrato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipocontrato.errors.count, "error") %> prohibited this tipocontrato from being saved:</h2>

      <ul>
      <% tipocontrato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
